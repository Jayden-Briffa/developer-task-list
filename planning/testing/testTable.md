<!-- TODO: Add hardware env, data size, and repetitions to performance tests -->

## Values
- taskById{standard}= {1: Task{name:"task1"}, 2: Task{name:"task2"}}
- taskIdsByName{standard}= {"task1": 1, "task2": 2}
- taskIdsByCategory{standard}= {
    "category1": [1],
    "category2": [2]
}

## Unit tests
| Test # | Depends on ids | Test type | Requirement links | Function | Description | Inputs | Expected results |
|---|---|---|---|---|---|---|---|
| 1 | — | Normal | FR3 | `loadTasks` | Accurately retrieves data from `tasks.json` and returns appropriate task storage objects. | — | Data from `tasksById` reflects `tasks.json` identically, and `taskIdsByName` and `taskIdsByCategory` map name and category to the correct id. |
| 2 | 1 | Normal | FR3 | `saveTasks` | Accurately overwrites `tasks.json` with the data from `tasksById`. | `tasksById= taskById{standard}` | Data in `tasks.json` matches: `taskById{standard} `|

## End-to-end tests
| Test # | Depends on ids | Test type | Requirement links | Function | Description | Inputs | Expected results |
|---|---|---|---|---|---|---|---|
| 3 | 1 | Normal | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects. | `tasksById= taskById{standard}` `newTask= {name: "name1", description: "desc1", deadline: "15/12/2026", category: "category1"}` | `tasksById` is updated to include id `3`; `taskIdsByName` is updated to include `"name1": 3`; and `taskIdsByCategory["category1"]` is updated to include `3`. |
| 4 | 3 | Boundary (lower) | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects when given minimal but valid data. | `tasksById= taskById{standard}` `newTask= {name: "a"*3, description: "", category: "cat"}` | `tasksById` is updated to include id `3`; `taskIdsByName` is updated to include the new name mapped to `3`; and `taskIdsByCategory["cat"]` is created/updated to include `3`. |
| 5 | 3 | Boundary (upper) | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects when given maximal but valid data. | `tasksById= taskById{standard}` `newTask= {name: "a"*32, description: "a"*255, deadline: "15/12/2026", category: "a"*16}` | `tasksById` is updated to include id `3`; `taskIdsByName` is updated to include the new name mapped to `3`; and `taskIdsByCategory` is updated so the new category includes `3`. |
| 6 | 3 | Normal | FR1, FR2, NFR1 | `createNewTask` | If the entered category does not exist, asks the user whether to create it before creating the task. | `tasksById= taskById{standard}` `taskIdsByCategory= taskIdsByCategory{standard}` `newTask= {name: "name3", description: "desc3", category: "newcat"}` `userChoice= "yes"` | The following confirmation prompt is outputted:  `Category 'newcat' does not exist. Create it? (yes/no)`  `tasksById` includes the new task in category `newcat`; `taskIdsByName` is updated to include `"name3": 3`; and `taskIdsByCategory` is updated to include `"newcat": [3]`. |
| 7 | 3 | Normal | FR1, FR2, NFR1 | `createNewTask` | If the entered category does not exist and the user declines creation, does not create the task. | `tasksById= taskById{standard}` `taskIdsByCategory= taskIdsByCategory{standard}` `newTask= {name: "name3", description: "desc3", category: "newcat"}` `userChoice= "no"` | The following confirmation prompt is outputted:  `Category 'newcat' does not exist. Create it? (yes/no)`  `tasksById`, `taskIdsByName`, and `taskIdsByCategory` are unchanged and the user can re-enter a category. |
| 8 | 3 | Invalid / Boundary (lower) | NFR1, NFR2 | `createNewTask` | Appropriate error messages are output when minimal invalid inputs are given and the user may retry input. | `newTask= {name: "ta", deadline: "invalid", category: "ca"}` | The following error messages are outputted and the user can retry their inputs:  `Field 'name' must be between 3 and 32 characters long (inclusive)`  `Field 'deadline' must be a date in the format dd/mm/yyyy`  `Field 'category' must be between 3 and 16 characters long (inclusive)` |
| 9 | 3 | Invalid / Boundary (upper) | NFR1, NFR2 | `createNewTask` | Appropriate error messages are output when maximal invalid inputs are given and the user may retry input. | `newTask= {name: "a"*33, description: "a"*256, category: "a"*17}` | The following error messages are outputted and the user can retry their inputs:  `Field 'name' must be between 3 and 32 characters long (inclusive)`  `Field 'description' must be less than or equal to 255 characters long`  `Field 'category' must be between 3 and 16 characters long (inclusive)` |
| 10 | 1 | Normal | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` with the given task data. | `tasksById= taskById{standard}` `taskId= 1` `updatedTask= {name:"newTask1", category:"category2"}` | `tasksById[1]` is updated with the new values; `taskIdsByName` removes/updates the old name mapping and includes `"newTask1": 1`; and `taskIdsByCategory` moves id `1` to `"category2"` (removing it from the previous category). |
| 11 | 10 | Boundary (lower) | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` when given minimal but valid task data. | `tasksById= taskById{standard}` `taskId= 1` `updatedTask= {name: "a"*3, description: "", category: "cat"}` | `tasksById[1]` is updated with the new values; `taskIdsByName` is updated so the new name maps to `1`; and `taskIdsByCategory` moves id `1` to `"cat"` correctly. |
| 12 | 10 | Boundary (upper) | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` when given maximal but valid task data. | `tasksById= taskById{standard}` `taskId= 1` `updatedTask= {name: "a"*32, description: "a"*255, deadline: "15/12/2026", category: "a"*16}` | `tasksById[1]` is updated with the new values; `taskIdsByName` is updated so the new name maps to `1`; and `taskIdsByCategory` is updated so id `1` appears in the new category. |
| 13 | 10 | Normal | FR1, FR2, NFR1 | `updateTask` | If the updated category does not exist, asks the user whether to create it before updating the task. | `tasksById= taskById{standard}` `taskIdsByCategory= taskIdsByCategory{standard}` `taskId= 1` `updatedTask= {name:"task1", category:"newcat"}` `userChoice= "yes"` | The following confirmation prompt is outputted:  `Category 'newcat' does not exist. Create it? (yes/no)`  `tasksById[1].category` is updated to `newcat`; `taskIdsByCategory` is updated to include `"newcat": [1]` and remove id `1` from its old category; and `taskIdsByName` remains correct for task id `1` (or is updated if name changed). |
| 14 | 10 | Normal | FR1, FR2, NFR1 | `updateTask` | If the updated category does not exist and the user declines creation, does not update the task category. | `tasksById= taskById{standard}` `taskIdsByCategory= taskIdsByCategory{standard}` `taskId= 1` `updatedTask= {name:"task1", category:"newcat"}` `userChoice= "no"` | The following confirmation prompt is outputted:  `Category 'newcat' does not exist. Create it? (yes/no)`  `tasksById`, `taskIdsByName`, and `taskIdsByCategory` are unchanged and the user can re-enter a category. |
| 15 | 10 | Invalid / Boundary (lower) | NFR1, NFR2 | `updateTask` | Appropriate error messages are output when minimal invalid task data is given and the user may retry input. | `taskId= 1` `updatedTask= {name: "ta", deadline: "invalid", category: "ca"}` | The following error messages are outputted and the user can retry their inputs:  `Field 'name' must be between 3 and 32 characters long (inclusive)`  `Field 'deadline' must be a date in the format dd/mm/yyyy`  `Field 'category' must be between 3 and 16 characters long (inclusive)`  `tasksById`, `taskIdsByName`, and `taskIdsByCategory` are unchanged |
| 16 | 10 | Invalid / Boundary (upper) | NFR1, NFR2 | `updateTask` | Appropriate error messages are output when maximal invalid task data is given and the user may retry input. | `taskId= 1` `updatedTask= {name: "a"*33, description: "a"*256, category: "a"*17}` | The following error messages are outputted and the user can retry their inputs:  `Field 'name' must be between 3 and 32 characters long (inclusive)`  `Field 'description' must be less than or equal to 255 characters long`  `Field 'category' must be between 3 and 16 characters long (inclusive)`  `tasksById`, `taskIdsByName`, and `taskIdsByCategory` are unchanged |
| 17 | 1 | Normal | FR1 | `deleteTask` | Removes the task with the given id from all task storage objects. | `taskId: 1` | `tasksById` removes id `1`; `taskIdsByName` removes the deleted task's name mapping; and `taskIdsByCategory` removes id `1` from its category list (removing the category key if it becomes empty). |
| 18 | 1 | Invalid | FR1 | `deleteTask` | Appropriate error messages are output when nonexistent IDs are given | `taskId: "invalid"` | `tasksById`, `taskIdsByName`, and `taskIdsByCategory` remain unchanged, and the following error is outputted:  `Given id, 'invalid', does not exist` |
| 19 | 1 | Normal | FR1, NFR1 | `outputTasks` | Displays all tasks, separated by category. | — | Outputs a clearly segmented task list as shown in *Appendix 2: Interface notes* |
| 20 | 1 | Normal | FR1, NFR1 | `outputTask` | Displays all data for the given task | `taskId: 1` | Outputs a clear task with the full description as shown in *Appendix 2: Interface notes* |  

## Non-functional tests
| Test # | Depends on ids | Test type | Requirement links | Function | Description | Inputs | Expected results |
|---|---|---|---|---|---|---|---|
| 21 | 1 | Extreme | NFR2 | `loadTasks` | Handles unexpected file read/parse failures without crashing and returns control to the user with actionable feedback. | `tasks.json` missing, unreadable, or malformed JSON | Program does not crash; storage objects remain unchanged (or initialize safely to empty); actionable error message is output (for example: `Unable to load tasks from tasks.json. Please verify file permissions and JSON format.`). |
| 22 | 3 | Normal | NFR3 | `createNewTask` | Adding a task completes within the required time limit under normal load. | Preload storage objects with 1000 valid tasks and add one valid task | Task is added correctly across `tasksById`, `taskIdsByName`, and `taskIdsByCategory`, and elapsed time is `<= 0.5s`. |
| 23 | 19, 20 | Non-functional (performance) | NFR3 | `outputTask`, `outputTasks` | Reading tasks by id and by category completes within the required time limit under normal load. | Preload storage objects with 1000 valid tasks across multiple categories. Read one task by id and output tasks grouped by category. | Both read operations return correct data and each measured elapsed time is `<= 0.5s`. |

When 1000 tasks across 20 categories are loaded, all model state management methods (createTask, saveTasks, etc.) task less than 0.5s to complete