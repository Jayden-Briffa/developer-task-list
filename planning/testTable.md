## Values
taskById{standard}= {1: Task{name:"task1"}, 2: Task{name:"task2"}}
taskIdsByName{standard}= {"task1": 1, "task2": 2}
taskIdsByCategory{standard}= {
    "category1": [1],
    "category2": [2]
}

## Unit tests

| Test # | Depends on ids | Test type | Requirement links | Function | Description | Inputs | Expected results |
|---|---|---|---|---|---|---|---|
| — | — | Normal | FR3 | `loadTasks` | Accurately retrieves data from `tasks.json` and returns appropriate task storage objects. | — | Data from `tasksById` reflects `tasks.json` identically, and `taskIdsByName` and `taskIdsByCategory` map name and category to the correct id. |
| — | — | Normal | FR3 | `saveTasks` | Accurately overwrites `tasks.json` with the data from `tasksById`. | `tasksById= taskById{standard}` | Data in `tasks.json` matches: `taskById{standard} `|
| — | — | Normal | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects. | `tasksById= taskById{standard}`<br>`newTask= {name: "name1", description: "desc1", deadline: "15/12/2026", category: "category1"}` | `tasksById` is updated to: `{1: {name:"task1"}, 2: {name:"task2"}, 3: {name:"name1", description: "desc1", deadline: "15/12/2026", category:"category1"}}` |
| — | — | Boundary (lower) | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects when given minimal but valid data. | `tasksById= taskById{standard}`<br>`newTask= {name: "a"*3, description: "", category: "cat"}` | `tasksById` is updated to: `{1: {name:"task1"}, 2: {name:"task2"}, 3: {name:"a"*3, description: "", category:"cat"}}` |
| — | — | Boundary (upper) | FR1, FR2 | `createNewTask` | Adds the task with the given information to the task storage objects when given maximal but valid data. | `tasksById= taskById{standard}`<br>`newTask= {name: "a"*32, description: "a"*255, deadline: "15/12/2026", category: "a"*16}` | `tasksById` is updated to: `{1: {name:"task1"}, 2: {name:"task2"}, 3: {name:"a"*32, description: "a"*255, deadline: "15/12/2026", category:"a"*16}}` |
| — | — | Normal | FR1, FR2, NFR1 | `createNewTask` | If the entered category does not exist, asks the user whether to create it before creating the task. | `tasksById= taskById{standard}`<br>`taskIdsByCategory= taskIdsByCategory{standard}`<br>`newTask= {name: "name3", description: "desc3", category: "newcat"}`<br>`userChoice= "yes"` | The following confirmation prompt is outputted:<br><br>`Category 'newcat' does not exist. Create it? (yes/no)`<br><br>`tasksById` includes the new task in category `newcat` and `taskIdsByCategory` is updated to include `"newcat": [3]`. |
| — | — | Normal | FR1, FR2, NFR1 | `createNewTask` | If the entered category does not exist and the user declines creation, does not create the task. | `tasksById= taskById{standard}`<br>`taskIdsByCategory= taskIdsByCategory{standard}`<br>`newTask= {name: "name3", description: "desc3", category: "newcat"}`<br>`userChoice= "no"` | The following confirmation prompt is outputted:<br><br>`Category 'newcat' does not exist. Create it? (yes/no)`<br><br>`tasksById` and `taskIdsByCategory` are unchanged and the user can re-enter a category. |
| — | — | Invalid / Boundary (lower) | NFR1, NFR2 | `createNewTask` | Appropriate error messages are output when minimal invalid inputs are given and the user may retry input. | `newTask= {name: "ta", deadline: "invalid", category: "ca"}` | The following error messages are outputted and the user can retry their inputs:<br><br>`Field 'name' must be between 3 and 32 characters long (inclusive)`<br><br>`Field 'deadline' must be a date in the format dd/mm/yyyy`<br><br>`Field 'category' must be between 3 and 16 characters long (inclusive)` |
| — | — | Invalid / Boundary (upper) | NFR1, NFR2 | `createNewTask` | Appropriate error messages are output when maximal invalid inputs are given and the user may retry input. | `newTask= {name: "a"*33, description: "a"*256, category: "a"*17}` | The following error messages are outputted and the user can retry their inputs:<br><br>`Field 'name' must be between 3 and 32 characters long (inclusive)`<br><br>`Field 'description' must be less than or equal to 255 characters long`<br><br>`Field 'category' must be between 3 and 16 characters long (inclusive)` |
| — | — | Normal | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` with the given task data. | `tasksById= taskById{standard}`<br>`taskId= 1`<br>`updatedTask= {name:"newTask1", category:"category2"}` | `tasksById` is updated to:<br>`{1: {name:"newTask1", category:"category2"...}, 2: {name:"task2"...}}` |
| — | — | Boundary (lower) | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` when given minimal but valid task data. | `tasksById= taskById{standard}`<br>`taskId= 1`<br>`updatedTask= {name: "a"*3, description: "", category: "cat"}` | `tasksById` is updated to:<br>`{1: {name:"a"*3, description:"", category:"cat"}, 2: {name:"task2"}}` |
| — | — | Boundary (upper) | FR1, FR2 | `updateTask` | Replaces the information in `tasksById` when given maximal but valid task data. | `tasksById= taskById{standard}`<br>`taskId= 1`<br>`updatedTask= {name: "a"*32, description: "a"*255, deadline: "15/12/2026", category: "a"*16}` | `tasksById` is updated to:<br>`{1: {name:"a"*32, description:"a"*255, deadline: "15/12/2026", category:"a"*16}, 2: {name:"task2"}}` |
| — | — | Normal | FR1, FR2, NFR1 | `updateTask` | If the updated category does not exist, asks the user whether to create it before updating the task. | `tasksById= taskById{standard}`<br>`taskIdsByCategory= taskIdsByCategory{standard}`<br>`taskId= 1`<br>`updatedTask= {name:"task1", category:"newcat"}`<br>`userChoice= "yes"` | The following confirmation prompt is outputted:<br><br>`Category 'newcat' does not exist. Create it? (yes/no)`<br><br>`tasksById[1].category` is updated to `newcat` and `taskIdsByCategory` is updated to include `"newcat": [1]`. |
| — | — | Normal | FR1, FR2, NFR1 | `updateTask` | If the updated category does not exist and the user declines creation, does not update the task category. | `tasksById= taskById{standard}`<br>`taskIdsByCategory= taskIdsByCategory{standard}`<br>`taskId= 1`<br>`updatedTask= {name:"task1", category:"newcat"}`<br>`userChoice= "no"` | The following confirmation prompt is outputted:<br><br>`Category 'newcat' does not exist. Create it? (yes/no)`<br><br>`tasksById` and `taskIdsByCategory` are unchanged and the user can re-enter a category. |
| — | — | Invalid / Boundary (lower) | NFR1, NFR2 | `updateTask` | Appropriate error messages are output when minimal invalid task data is given and the user may retry input. | `taskId= 1`<br>`updatedTask= {name: "ta", deadline: "invalid", category: "ca"}` | The following error messages are outputted and the user can retry their inputs:<br><br>`Field 'name' must be between 3 and 32 characters long (inclusive)`<br><br>`Field 'deadline' must be a date in the format dd/mm/yyyy`<br><br>`Field 'category' must be between 3 and 16 characters long (inclusive)`<br><br>`tasksById` is unchanged |
| — | — | Invalid / Boundary (upper) | NFR1, NFR2 | `updateTask` | Appropriate error messages are output when maximal invalid task data is given and the user may retry input. | `taskId= 1`<br>`updatedTask= {name: "a"*33, description: "a"*256, category: "a"*17}` | The following error messages are outputted and the user can retry their inputs:<br><br>`Field 'name' must be between 3 and 32 characters long (inclusive)`<br><br>`Field 'description' must be less than or equal to 255 characters long`<br><br>`Field 'category' must be between 3 and 16 characters long (inclusive)`<br><br>`tasksById` is unchanged |
| — | — | Normal | FR1 | `deleteTask` | Removes the task with the given id from all task storage objects. | `taskId: 1` | tasksById is updated to `{2: {name:"task2"...}}` |
| — | — | Invalid | FR1 | `deleteTask` | Appropriate error messages are output when nonexistent IDs are given | `taskId: "invalid"` | Storage objects remain the same and the following error is outputted:<br><br>`Given id, 'invalid', does not exist` |
| — | — | Normal | FR1, NFR1 | `outputTasks` | Displays all tasks, separated by category. | — | Outputs a clearly segmented task list as shown in REPLACE_PLACE_NAME |
| — | — | Normal | FR1, NFR1 | `outputTask` | Displays all data for the given task | `taskId: 1` | Outputs a clear task with the full description as shown in REPLACE_PLACE_NAME |  

> **__NOTE:__** Add unit tests
> **__NOTE:__** Add non-functional tests